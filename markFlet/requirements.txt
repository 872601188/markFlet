flet>=0.20.0
markdown>=3.5.0
python-docx>=1.1.0
pymdown-extensions>=10.0
beautifulsoup4>=4.12.0
Pillow>=10.0.0
weasyprint>=60.0; platform_system != "Windows"  # PDF 导出（Windows 需要额外安装 GTK 运行时）
# pdfkit>=1.0.0  # 可选方案，需要安装 wkhtmltopdf